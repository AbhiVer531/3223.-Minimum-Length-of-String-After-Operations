class Solution:
    def minimumLength(self, s: str) -> int:
        sum = 0
        for x in Counter(s).values():
            if x%2 == 1:
                sum = sum+1
            else:
                sum = sum+2
        return sum
